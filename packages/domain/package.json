{
  "name": "@dnc/domain",
  "version": "0.0.1",
  "private": true,
  "description": "Framework-free domain rules shared by api, web and mobile. Source-only: consumed by Turbopack, Metro and vite-node, never executed as raw Node ESM, so it is deliberately not declared \"type\": \"module\" \u2014 that would make TypeScript's nodenext mode demand .js specifiers that Turbopack and Metro cannot resolve back to .ts.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@dnc/contracts": "workspace:*"
  },
  "devDependencies": {
    "@dnc/config": "workspace:*",
    "typescript": "7.0.2",
    "vitest": "4.1.11"
  }
}
