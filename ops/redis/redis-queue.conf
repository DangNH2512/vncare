# Queue/bus Redis instance. BullMQ requires noeviction: with an eviction
# policy, jobs and unconsumed messages disappear silently under memory
# pressure. AOF persistence survives container restarts.
maxmemory 512mb
maxmemory-policy noeviction
appendonly yes
appendfsync everysec
